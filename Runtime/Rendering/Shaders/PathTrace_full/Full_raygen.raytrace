// Constraints:
// Cannot handle rough (smoothness < 1) transparent surfaces
// Cannot handle perfect metal (metallic = 1, smoothness = 1) opaque surfaces

#pragma use_dxc

// Uncomment this pragma for debugging the HLSL code in PIX. GPU performance will be impacted.
#pragma enable_ray_tracing_shader_debug_symbols
#pragma max_recursion_depth 1

#include "Packages/com.unity.render-pipelines.universal/ShaderLibrary/Core.hlsl"
#include "Packages/com.unity.render-pipelines.core/ShaderLibrary/Packing.hlsl"
#include "Packages/com.unity.render-pipelines.universal/ShaderLibrary/Lighting.hlsl"
#include "Packages/com.unity.render-pipelines.universal/ShaderLibrary/GlobalIllumination.hlsl"
#include "Assets/Caelix/VoxelMaterials.hlsl"
#include "../RayPayload.hlsl"
#include "../Utils/Utils.hlsl"
#include "../Utils/BlueNoise.hlsl"
#include "../def/CaelixRayPayloadUtils.hlsl"
#include "../def/CaelixUtils.hlsl"

RaytracingAccelerationStructure g_AccelStruct : register(t0, space1);

#define CAELIX_TRACE_RAY(ray, payload) TraceRay(g_AccelStruct, RAY_FLAG_FORCE_OPAQUE, 0xFF, 0, 1, 0, ray, payload)
#define CAELIX_LAUNCH_INDEX (DispatchRaysIndex().xy)
#define CAELIX_LAUNCH_DIM (DispatchRaysDimensions().xy)

#include "CaelixPathTraceCommon.hlsl"

[shader("raygeneration")]
void MainRayGenShader()
{
    CaelixPathTraceMain();
}

[shader("miss")]
void MainMissShader0(inout RayPayload payload: SV_RayPayload)
{
    CaelixApplyVoxelMiss(payload);
}
